CollectorHub Update31 REPAIR SAFE

Это безопасный ремонтный пакет. Он НЕ содержит main.py, чтобы снова не сломать рабочие кнопки Update 27.

Что исправлено:
1. facebook_group_search.py теперь принимает запросы В ОДНУ СТРОКУ через запятую.
   Пример:
   Świdnica praca, Wałbrzych ogłoszenia, Українці Świdnica, работа Wałbrzych

2. Каждый кусок между запятыми запускается как отдельный поиск Facebook-групп.

3. Скрипт сам:
   - ищет группы;
   - пропускает уже добавленные;
   - пробует вступить;
   - если нужны вопросы — ставит pending_questions;
   - добавляет найденные группы в CollectorHub список.

Как ставить:
1. Сначала сделай копию всей рабочей папки CollectorHub.
2. Скопируй файлы из этого архива поверх файлов в папке CollectorHub.
3. Папку data НЕ удалять и НЕ заменять.
4. Запусти проверку:
   python verify_collectorhub.py

Если кнопка 'Поиск Facebook Group' уже есть в интерфейсе — она будет работать с запросами через запятую.
Если кнопки нет — не меняй main.py вслепую, сначала пришли рабочий main.py Update 27.
